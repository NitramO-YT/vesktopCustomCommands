[ ![🇬🇧 English](https://img.shields.io/badge/%F0%9F%87%AC%F0%9F%87%A7-English-blue) ](README.md) [ ![🇫🇷 Français](https://img.shields.io/badge/%F0%9F%87%AB%F0%9F%87%B7-Fran%C3%A7ais-blue) ](README.fr.md) [ ![🇪🇸 Español](https://img.shields.io/badge/%F0%9F%87%AA%F0%9F%87%B8-Espa%C3%B1ol-blue) ](README.es.md) [ ![🇩🇪 Deutsch](https://img.shields.io/badge/%F0%9F%87%A9%F0%9F%87%AA-Deutsch-blue) ](README.de.md) [ ![🇮🇹 Italiano](https://img.shields.io/badge/%F0%9F%87%AE%F0%9F%87%B9-Italiano-blue) ](README.it.md) [ ![🇯🇵 日本語](https://img.shields.io/badge/%F0%9F%87%AF%F0%9F%87%B5-%E6%97%A5%E6%9C%AC%E8%AA%9E-blue) ](README.ja.md) [ ![🇨🇳 中文](https://img.shields.io/badge/%F0%9F%87%A8%F0%9F%87%B3-%E4%B8%AD%E6%96%87-blue) ](README.zh.md) [ ![🇰🇷 한국어](https://img.shields.io/badge/%F0%9F%87%B0%F0%9F%87%B7-%ED%95%9C%EA%B5%AD%EC%96%B4-blue) ](README.ko.md)

# Введение в vesktopCustomCommands (VCC)
VCC — это система, которая позволяет вам добавить глобальные горячие клавиши для отключения микрофона и звука в Vesktop. Это временное решение для отсутствия глобальных горячих клавиш в Vesktop на данный момент, пока команда Vesktop не найдет лучшее решение.
По сути, это набор скриптов (`mute.sh` и `deafen.sh`), которые вы можете вызывать из пользовательских глобальных горячих клавиш в вашей системе для отключения микрофона и звука в Vesktop, и они запускают эти действия в Vesktop путем внедрения пользовательского кода Javascript в файл предзагрузки Vencord.

# Настройка горячих клавиш в вашей системе
Вам необходимо настроить пользовательскую глобальную горячую клавишу в вашей системе для вызова скриптов `mute.sh` и `deafen.sh` в папке `~/.vesktopCustomCommands/`.
```plaintext
~/.vesktopCustomCommands/mute.sh
```
```plaintext
~/.vesktopCustomCommands/deafen.sh
```

---

# Установка

## Автоматическая установка
Выполните эту команду в терминале и следуйте инструкциям:
```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/install.sh)"
```
Примечание: Если файл конфигурации уже существует в `~/.vesktopCustomCommands/.config`, установщик сохраняет его и обновляет только запись `vencord_path` при необходимости.

### Необязательно: Автоматическое повторное исправление

Во время установки вы можете включить систему автоматического повторного исправления, которая периодически проверяет, присутствует ли патч VCC в файле предзагрузки Vencord, и повторно применяет его, если он был удален (например, после обновления или сброса Vencord/Vesktop).

- Зачем это нужно? Обновления Vesktop/Vencord или определенные сценарии запуска могут восстановить файл предзагрузки в исходное состояние, удаляя внедрение VCC. Автоматическое повторное исправление гарантирует, что ваши горячие клавиши продолжат работать без ручного вмешательства.
- Настройки хранятся в `~/.vesktopCustomCommands/.config`:
  - `auto_repatch="true|false"` (по умолчанию: `false`)
  - `auto_restart="true|false"` (по умолчанию: `false`) – если включено, Vesktop будет автоматически перезапущен после повторного исправления. Вы можете включить/отключить это позже с помощью следующих команд.
  - `autorepatch_interval="30s|1m|3m"` (по умолчанию: `30s`) – интервал проверки.
  - Таймер пользователя `systemd` запускается с выбранным интервалом, когда включен `auto_repatch`.
  - Чтобы включить автоматическое повторное исправление:
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/enable_autorepatch.sh)"
  ```
  - Чтобы отключить автоматическое повторное исправление:
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/disable_autorepatch.sh)"
  ```

  - Чтобы включить автоматический перезапуск (после повторного исправления):
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/enable_autorestart.sh)"
  ```
  - Чтобы отключить автоматический перезапуск:
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/disable_autorestart.sh)"
  ```


Ручная настройка: отредактируйте `~/.vesktopCustomCommands/.config` и установите `auto_repatch` и `auto_restart`. Вы также можете установить `autorepatch_interval` на `"30s"`, `"1m"` или `"3m"`. Если вы отключите его вручную, таймер будет остановлен при следующем запуске установки, или выполните приведенный выше скрипт отключения.

### Необязательно: Автоматическое обновление

Вы можете включить систему автоматического обновления, которая периодически проверяет, доступна ли более новая версия на GitHub, и обновляет необходимые файлы (пользовательский код для Vencord и локальные скрипты, такие как `mute.sh` и `deafen.sh`).

- Настройки в `~/.vesktopCustomCommands/.config`:
  - `auto_update="true|false"` (по умолчанию: `false`)
  - `auto_update_interval` (по умолчанию: `15m`) – таймер запускается с `autorepatch_interval`, если включено автоматическое повторное исправление, в противном случае с `auto_update_interval`, если включено только автоматическое обновление.
- Чтобы включить автоматическое обновление:
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/enable_autoupdate.sh)"
  ```
- Чтобы отключить автоматическое обновление:
  ```bash
  bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/dist/vesktopCustomCommands/disable_autoupdate.sh)"
  ```

## Ручная установка
1. Загрузите папку `dist` из репозитория или ее содержимое.
2. `dist` разделен на две части:
    - Папка `vencord` содержит файлы для внедрения в файл предзагрузки Vencord.
    - Папка `vesktopCustomCommands` содержит скрипты для отключения микрофона/звука и файл `.config`.
3. Вы можете сделать резервную копию вашего файла предзагрузки Vencord (обычно расположенного в `~/.config/Vencord/dist/vencordDesktopPreload.js` с помощью `cp ~/.config/Vencord/dist/vencordDesktopPreload.js ~/.config/Vencord/dist/vencordDesktopPreload.js.bak`) или нет, если вы хотите восстановить его позже, вы можете удалить файл и запустить Vesktop для его пересоздания.
4. Внедрите содержимое `vencordDesktopPreload_sample.js` в ваш файл предзагрузки Vencord (обычно расположенный в `~/.config/Vencord/dist/vencordDesktopPreload.js`). Метод внедрения зависит от вашей версии Vencord:
    - **СТАРАЯ структура Vencord (версия 21e6178 или старше):** Найдите строку `document.addEventListener("DOMContentLoaded",()=>document.documentElement.appendChild(r),{once:!0})` и замените ее на `document.addEventListener("DOMContentLoaded",()=>{document.documentElement.appendChild(r);(СОДЕРЖИМОЕ ОБРАЗЦА ФАЙЛА ПРЕДЗАГРУЗКИ ЗДЕСЬ)},{once:!0})`, затем замените `(СОДЕРЖИМОЕ ОБРАЗЦА ФАЙЛА ПРЕДЗАГРУЗКИ ЗДЕСЬ)` на содержимое `vencordDesktopPreload_sample.js`.
    - **НОВАЯ структура Vencord (версия c123efd или новее):** Найдите текст `getTheme",s.quickCss.getEditorTheme))` и замените его на `getTheme",s.quickCss.getEditorTheme));if(location.protocol!=="data:"){document.readyState==="complete"?(()=>{(СОДЕРЖИМОЕ ОБРАЗЦА ФАЙЛА ПРЕДЗАГРУЗКИ ЗДЕСЬ)})():document.addEventListener("DOMContentLoaded",()=>{(СОДЕРЖИМОЕ ОБРАЗЦА ФАЙЛА ПРЕДЗАГРУЗКИ ЗДЕСЬ)},{once:!0})}`, затем замените `(СОДЕРЖИМОЕ ОБРАЗЦА ФАЙЛА ПРЕДЗАГРУЗКИ ЗДЕСЬ)` на содержимое `vencordDesktopPreload_sample.js`.
    - **Альтернатива:** Замените весь файл предоставленным `vencordDesktopPreload.js` (*НЕ РЕКОМЕНДУЕТСЯ, так как в случае обновления Vesktop, если VCC не обновлялся с тех пор, это менее надежно, и этот файл может быть устаревшим*).
5. Создайте каталог `vesktopCustomCommands` в вашем пути Vencord (обычно расположенном в `~/.config/Vencord/dist/`) и поместите в него файл `customCode.js`.
6. Создайте каталог `~/.vesktopCustomCommands` и поместите в него файлы `mute.sh` и `deafen.sh`.
7. Добавьте разрешения для скриптов `mute.sh` и `deafen.sh`:
    ```bash
    chmod +x ~/.vesktopCustomCommands/mute.sh
    chmod +x ~/.vesktopCustomCommands/deafen.sh
    ```
8. Поместите файл `.config` в `~/.vesktopCustomCommands` и обновите переменную `vencord_path` с вашим путем Vencord при необходимости.
9. Перезапустите Vesktop, чтобы применить изменения.
10. Настройте пользовательскую глобальную горячую клавишу в вашей системе для вызова скриптов `mute.sh` и `deafen.sh` в папке `~/.vesktopCustomCommands/`.
    - `mute.sh` для отключения микрофона. `~/.vesktopCustomCommands/mute.sh`
    - `deafen.sh` для отключения звука. `~/.vesktopCustomCommands/deafen.sh`
11. Наслаждайтесь вашими новыми глобальными горячими клавишами для отключения микрофона и звука!

---

# Удаление

## Автоматическое удаление

Выполните эту команду в терминале и следуйте инструкциям:
```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/NitramO-YT/vesktopCustomCommands/refs/heads/main/uninstall.sh)"
```
Во время удаления вас спросят, хотите ли вы удалить ВСЕ, включая ваши настройки (`~/.vesktopCustomCommands/.config`).
- Ответьте "y": все файлы и настройки удаляются.
- Ответьте "n": удаляются только файлы программы; ваш `.config` сохраняется.

Если настройки удалены, служба/таймер автоматического повторного исправления и вспомогательные скрипты также удаляются. Если вы откажетесь от автоматического удаления, следуйте приведенным ниже шагам ручного удаления (те же инструкции также выводятся скриптом).

## Ручное удаление

1. Удалите пользовательские глобальные горячие клавиши в вашей системе, которые вызывают скрипты `mute.sh` и `deafen.sh` в папке `~/.vesktopCustomCommands/`.
2. Удалите файл `.config` в `~/.vesktopCustomCommands`.
3. Удалите папку `~/.vesktopCustomCommands`.
4. Удалите файл `customCode.js` в вашем пути Vencord `~/.config/Vencord/dist/vesktopCustomCommands/`.
5. Удалите папку `vesktopCustomCommands` в вашем пути Vencord `~/.config/Vencord/dist/`.
6. Удалите внедренный код в вашем файле предзагрузки Vencord (обычно расположенном в `~/.config/Vencord/dist/vencordDesktopPreload.js`) или замените его резервной копией, которую вы сделали, если она у вас есть. (Вы также можете удалить файл и запустить Vesktop для его пересоздания).
7. Перезапустите Vesktop, чтобы применить изменения.

---

# Проблемы и улучшения

Если у вас есть какие-либо проблемы или предложения по улучшению, откройте issue!

# Вклад

Я знаю, что эта система не идеальна и что я не соблюдал все стандарты и семантику, поэтому я рассчитываю на тех, кто хочет помочь мне улучшить эту систему. Issues и pull requests открыты, и я открыт для любой конструктивной критики!

---

# Объяснение основной цели этого проекта

Я был пользователем, привыкшим к KDE Neon под X11, и мой Discord работал хорошо в целом. Недавно я перешел на KDE Neon под Wayland и обнаружил, что Discord имел много проблем с ним, особенно демонстрация экрана была для меня невозможна. Ища решение моих проблем с Discord, я обнаружил Vesktop и, соответственно, Vencord, и я обнаружил все проблемы, которые он решил, и даже некоторые, которые у меня уже были под X11 (например, чистое и простое отсутствие возможности делиться звуком во время демонстрации экрана). Я установил его, и все было идеально, за исключением одной небольшой детали: отсутствия поддержки глобальных сочетаний клавиш. Единственной возможностью были стандартные сочетания клавиш Discord (`Ctrl + Shift + M` и `Ctrl + Shift + D`), которые работают только если окно активно. Поэтому я начал искать сочетания клавиш в Vesktop и мог видеть и читать, что проблема известна, но решение все еще далеко от того, чтобы быть найденным, особенно на Wayland, что, кажется, усложняет жизнь разработчикам, которые стремятся создать глобальные сочетания клавиш. Поэтому я подумал о том, чтобы сдаться, но как любой хороший разработчик, я не мог смириться, поэтому я подумал о решении и нашел временное, но надежное решение. Я не хотел нестабильной системы, поэтому я попытался сделать свою систему максимально простой и функциональной. Я мог бы форкнуть Vesktop и усердно работать, чтобы найти решение или интегрировать свое, но у меня больше нет этих претензий и времени, и кроме того, я не думаю, что серьезно или здорово создавать альтернативный репозиторий для людей, которые хотят глобальные сочетания клавиш в своем Vesktop. Поэтому я подумал, что идеально было бы думать об этом как о моде или дополнении, которое добавляется поверх официального, немного как сам Vencord в конце концов, что заинтересованные люди могут установить его, если им это нужно со своей стороны. Этого будет достаточно, пока Vesktop не найдет решение для глобальных сочетаний клавиш!

---

Спасибо :)



<!-- Made with ❤️ by NitramO -->
